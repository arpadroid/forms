/**
 * Storybook configuration for the Arpadroid module.
 * This file imports the main Storybook configuration from the module and exports it.
 * Add your Storybook configuration overrides here if needed.
 */
import config from '@arpadroid/module/storybook/main';
export default { ...config };
