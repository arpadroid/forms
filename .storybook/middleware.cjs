/**
 * Storybook middleware configuration.
 * This file imports the middleware configuration from the module and exports it.
 * Add your Storybook middleware configuration overrides here if needed.
 */
const middleWare = require('@arpadroid/module/storybook/middleware');
module.exports = middleWare;
